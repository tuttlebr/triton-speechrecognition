apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triton-speech-recognition.fullname" . }}-test-connection"
  labels:
    {{- include "triton-speech-recognition.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "triton-speech-recognition.fullname" . }}:{{ .Values.serviceFrontend.nodePort }}']
  restartPolicy: Never
